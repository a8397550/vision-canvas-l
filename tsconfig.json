{
    "compilerOptions": {
      "jsx": "react", // "React"或"Preserve" 在.tsx文件中支持JSX
      "target": "es5", // 指定ECMAScript的目标版本："ES3"（默认）， "ES5"，"ES6"/ "ES2015"，"ES2016"，"ES2017"或"ESNext"
      "noImplicitAny": false, // 使用隐含any类型提高表达式和声明的错误。
      "removeComments": true, // 删除所有注释，但以复制权标题注释开头除外
      "emitDecoratorMetadata": true, // 在源中为装饰声明发出设计类型元数据。
      "experimentalDecorators": true, // 为ES装饰器启用实验支持。
      "pretty": true, // 使用颜色和上下文来设置错误和消息的样式。
      "baseUrl": "./src", // 用于解析非相对模块名称的基目录。
      /*
        指定模块代码生成："None"，"CommonJS"，"AMD"，"System"，"UMD"，"ES6"，"ES2015"或"ESNext"。
        ►仅"AMD"和"System"可以一起使用--outFile。
        ► 定位或降低时可以使用值"ES6"和"ES2015"值"ES5"。
        target === "ES3" or "ES5" ? "CommonJS" : "ES6"
      */
      "module": "esnext",
      /*
        确定如何解析模块  module === "AMD" or "System" or "ES6" ? "Classic" : "Node"
      */
      "moduleResolution": "node",
      /*
        指定包含
      */
      "typeRoots": [
        "./node_modules/@types",
        "./types"
      ]
    },
    "exclude": [ // 不包含以下目录
      "node_modules",
      "dist"
    ]
  }